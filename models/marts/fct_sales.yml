models:
  - name: fct_sales
    description: Sales fact table as wished by the business.
    columns:
      - name: sk_sale
        description: "{{ doc('sk_sale') }}" 
        tests:
          - unique
          - not_null
      - name: sales_order_detail_id
        description: "{{ doc('sales_order_detail_id') }}" 
        tests:
          - unique
          - not_null
      - name: sales_order_id
        description: "{{ doc('sales_order_id') }}" 
        tests:
          - not_null